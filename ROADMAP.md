# Atri Games 实施清单

清单按依赖顺序排列。每一阶段满足验收标准后再扩大范围。

## Phase 0：项目基础与决策

- [x] 克隆并连接 `sudoriaa/Atri-Games` 远端仓库。
- [x] 确定“Catalog + 独立游戏 Web App + 可选平台 API”的边界。
- [x] 明确游戏不需要 iframe、统一框架或统一后端。
- [x] 建立支持联网、自有后端和平台集成声明的 Manifest Schema。
- [x] 写出架构、平台集成、贡献流程和发布清单。
- [ ] 确认正式域名、门户部署提供商和首批 3 至 5 个种子游戏。
- [ ] 确认品牌视觉、目标用户、收录范围和内容规范。
- [ ] 创建 GitHub Project，并把本文件各阶段拆成 Issue。
- [ ] 配置分支保护：PR 合并、必需检查、禁止直接推送 main。

验收：开发者提供“HTTPS 游戏 URL + Manifest”或“静态浏览器构建 + Manifest”，无需迁移技术栈即可接入。

## Phase 1：可浏览、可启动的 MVP

### 工程基础

- [x] 初始化 pnpm workspace，锁定 Node 与 pnpm 版本。
- [x] 创建 `apps/web` 与 `apps/admin`，使用 React、Vite 和 TypeScript。
- [x] 建立 `packages/shared`、`packages/game-kit` 和 `packages/game-sdk`。
- [x] 配置类型检查、单元测试和生产构建检查。
- [x] 配置 CI：install、typecheck、test、build、Go test/vet 与容器冒烟。
- [x] 建立环境变量示例；确保客户端构建不包含密钥。

### Catalog

- [ ] 校验所有 `catalog/games/*/manifest.json`。
- [ ] 校验 ID 唯一、版本、HTTPS URL、标签、媒体和隐私字段。
- [ ] 从清单生成类型安全的游戏索引。
- [ ] 对坏清单给出包含文件名和字段路径的错误。
- [ ] 生成分类、标签、引擎、联网方式和设备兼容性聚合数据。
- [ ] 为封面和截图建立尺寸、格式与体积检查。

### 用户界面

- [ ] 游戏列表支持搜索、分类、标签、引擎、联网和设备筛选。
- [ ] 游戏卡片显示封面、标题、作者、标签和在线要求。
- [ ] 详情页显示说明、截图、版本、目标域名、隐私、许可证和源码链接。
- [x] “开始游戏”按 `launch.openIn` 直接打开独立游戏 URL。
- [x] 新标签页统一使用 `noopener noreferrer`。
- [ ] 桌面与移动视口无溢出、遮挡和文本截断。
- [ ] 键盘可以完成浏览、筛选、查看详情和启动游戏。
- [ ] 为无封面、空搜索、失效 URL 和健康检查失败建立完整状态。
- [ ] 生成标题、描述、Open Graph 图和 sitemap。

### 启动与链接安全

- [ ] 只接受 HTTPS 启动地址，限制重定向次数并记录最终域名。
- [ ] 重要游戏启用域名所有权验证。
- [ ] 启动链接不追加 Token、邮箱或其他个人数据。
- [ ] 记录启动点击后立即导航，不让分析请求阻塞玩家。
- [ ] 定时检查 DNS、TLS、状态码、重定向和基础页面可达性。
- [ ] 域名变化、证书异常或连续失败触发维护者提醒。
- [x] Manifest 内容经过严格 JSON 字段校验和 URL 协议 allowlist 处理。

### MVP 验收

- [ ] 至少收录 3 个不同技术栈的游戏。
- [ ] 至少一个游戏拥有自有后端或实时网络连接。
- [ ] 至少一个游戏为 Unity 或 Godot Web 构建。
- [ ] Chromium、Firefox、Safari 最新稳定版通过门户核心流程。
- [ ] 360x800、768x1024、1440x900 三组视口通过视觉检查。
- [ ] 目录搜索、详情、直接启动和失效链接有端到端测试。
- [ ] 门户有预览环境、生产环境和一键回滚。

## Phase 2：平台 API 与 Webhook

### 开发者控制台

- [ ] 登记应用、所属游戏、允许 Origins 和精确回调 URL。
- [ ] 申请并审核 Identity、Save、Achievement、Leaderboard Scopes。
- [ ] 管理 Webhook URL、订阅事件、签名密钥和轮换。
- [ ] 显示 API 请求 ID、Webhook 投递结果和重放工具。
- [ ] 所有敏感配置变更进入审计日志。

### Identity 与 API

- [ ] 使用 OpenID Connect Authorization Code + PKCE 提供 Atri 登录。
- [ ] 校验 issuer、audience、state/nonce、回调 URL 和 PKCE S256。
- [ ] 颁发短期、限 Scope、绑定用户和 `gameId` 的 Token。
- [ ] 按精确 Origin 配置 CORS，不允许带凭据的通配符来源。
- [ ] 实现版本化 JSON API、统一错误结构和请求 ID。
- [ ] 写接口支持幂等键、速率限制和配额。
- [ ] 游戏数据按用户与 `gameId` 隔离，并提供导出和删除。

### Webhook

- [ ] 定义事件信封、命名、版本和兼容规则。
- [ ] 每个游戏使用独立 HMAC 密钥，支持无中断轮换。
- [ ] 签名覆盖事件 ID、时间戳和原始请求体。
- [ ] 对失败投递指数退避，设置最大次数和存活时间。
- [ ] 支持事件 ID 去重、投递审计和开发者手动重放。
- [ ] 监控成功率、响应耗时、积压和失效端点。
- [ ] 文档提供 Node.js、Python、Go 和原生 Web 示例。

### 可选客户端库

- [x] 提供零依赖、无框架要求的可选浏览器生命周期 SDK，不要求基础收录使用。
- [ ] 提供服务端 Token 与 Webhook 验证库。
- [ ] 发布 OpenAPI、JSON Schema 和可独立运行的契约测试。
- [ ] SDK、API、Webhook 事件分别版本化并提供变更日志。

验收：独立游戏无需嵌入门户，即可安全使用 Atri 登录、API 和签名 Webhook。

## Phase 3：开发者生态与托管

- [ ] 建立 Manifest 示例和 HTML5、Phaser、Unity Web、Godot Web 发布指南。
- [x] 提供 CLI，生成并校验 Manifest、检查本地文件并构建 `.atri` 包。
- [x] 提供静态游戏可选托管，使用独立 Game Origin 与游戏 ID 命名空间。
- [x] 静态托管校验压缩包路径、符号链接、重复项、文件数量、大小、封面和入口页。
- [x] 明确平台静态托管只提供浏览器文件，不运行贡献者服务端代码。
- [ ] 编写联网、音频、输入、移动适配、隐私和故障状态指南。
- [ ] 公开收录、更新、下架、域名转移、所有权转移和争议政策。
- [ ] 建立维护者权限、CODEOWNERS、审核轮值和响应时间目标。
- [ ] 给作者页提供作品、主页、源码和许可证信息。
- [ ] 建立讨论区、提案模板、Good First Issue 和发布节奏。

验收：门户、游戏和平台 API 能独立发版，第三方维护者可以按文档完成收录审核。

## Phase 4：社区与运营能力

- [ ] 先写数据保留和隐私规则，再开放账户与社区数据。
- [ ] 实现开发者主页、收藏和最近游玩。
- [ ] 实现版本化云存档，限制单游戏配额并支持导出/删除。
- [ ] 实现成就和按游戏、模式、版本隔离的排行榜。
- [ ] 为排行榜定义服务端验证、重放或可信证明方案。
- [ ] 实现评分、评论、举报、审核记录和申诉流程。
- [ ] 对滥用接口配置限流、幂等、审计和封禁流程。
- [ ] 给用户提供数据导出、删除账号和隐私偏好入口。

验收：任何游戏都不能读取其他游戏数据；用户能查看和删除平台保存的数据。

## Phase 5：稳定性与规模化

- [ ] 定义门户可用性、启动链接健康率、API 延迟和 Webhook 成功率 SLO。
- [ ] 建立门户错误、外部链接、API、Webhook 和静态 CDN 仪表盘。
- [ ] 对 Catalog、对象存储、数据库和签名密钥执行恢复演练。
- [ ] 建立依赖更新、Secret 扫描、制品来源和依赖审计。
- [ ] 测试热门游戏流量、失效域名、Webhook 积压和恶意 API 请求。
- [ ] 按真实瓶颈增加队列、缓存或服务拆分，不预先微服务化。
- [ ] 公布状态页、事故流程和开发者通知机制。

验收：能从外部游戏失效、错误 Catalog 发布、Webhook 故障和主要存储故障中按目标时间恢复。

## 每个游戏的收录检查清单

- [ ] Manifest 通过 Schema，ID、版本、作者和许可证完整。
- [ ] 启动 URL 使用 HTTPS，直接访问、刷新和浏览器返回行为正常。
- [ ] 重定向最终域名、运营主体和提交信息一致。
- [ ] 封面、截图和描述与实际游戏一致。
- [ ] 键鼠、触控、手柄支持与 Manifest 声明一致。
- [ ] 联网、自有后端、实时技术和平台集成声明准确。
- [ ] 收集个人数据时提供可访问的隐私政策与数据说明。
- [ ] 断网、服务端错误和长时间加载有可理解的状态。
- [ ] 桌面和声明支持的移动浏览器完成直接启动测试。
- [ ] Manifest 与仓库中不包含 Token、Webhook 密钥或部署凭据。
