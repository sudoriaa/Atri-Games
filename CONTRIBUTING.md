# 贡献指南

感谢参与 Atri Games。平台接受任何可以作为独立网页访问的 AI 辅助创作游戏，不限定前端、引擎、服务端语言或部署平台。

## 可以贡献什么

- 提交一个新的独立网页游戏。
- 更新已收录游戏的版本、说明、启动 URL 或媒体。
- 改进门户、Catalog、平台 API、文档和工具。
- 报告链接、兼容性、隐私、可访问性、性能或内容问题。

## 游戏接入模型

接入有两个等价入口：

1. `runtime.kind=external`：发布完整游戏和后端，清单填写 HTTPS 地址。适合任何服务端语言、SSR、数据库、WebSocket、WebRTC 或 Webhook。
2. `runtime.kind=static`：运行 `npx @atri/game-kit pack` 生成 `.atri`，在管理后台导入。包内只放浏览器构建产物，平台不会运行服务端代码。

游戏不会被嵌入门户。玩家点击“开始游戏”后直接离开门户；静态包会先从门户 Origin 重定向到隔离 Game Origin。基础收录不要求 SDK；`@atri/game-sdk` 只提供可选生命周期消息。

## 游戏技术要求

### 必需

- 提供稳定的 HTTPS 启动 URL，直接访问后能进入有效游戏页面。
- 游戏独立部署，不依赖门户 DOM、Cookie、私有接口或构建变量。
- 浏览器刷新、后退和直接访问有可理解的行为。
- 清单中的作者、许可证、设备、输入、联网和数据收集信息真实完整。
- 自行保护服务端密钥、数据库和用户数据；公开仓库及 Manifest 不包含凭据。
- 收集个人数据时提供隐私政策、数据用途和支持渠道。
- 自行处理加载、断网、服务异常和版本回滚。

### 推荐

- 让主启动地址保持稳定，在游戏内部展示具体构建版本。
- 在用户交互后播放声音，并提供静音能力。
- 先加载启动所需资源，其他资源按需加载。
- 提供清晰的键盘焦点和足够大的触摸目标。
- 对 API 与实时连接设置超时、重连上限和可理解的错误状态。
- 提供返回 Atri Games 或项目主页的普通链接。

## 提交新游戏

1. Fork 本仓库并创建 `game/<game-id>` 分支，或在后台准备 `.atri`。
2. 从 `examples/game-manifest.json` 复制清单。
3. `static` 模式把构建产物放入 `game/`；`external` 模式填写已经部署的 HTTPS URL。
4. 确认 `id` 使用小写 kebab-case，`version` 对应当前游戏发布。
5. 填写 `runtime`、`services`、`privacy` 和实际兼容性。
6. 运行 `npx @atri/game-kit validate`，再用 `pack` 生成包（或按 Schema 校验清单）。
7. 从一个全新的浏览器会话直接打开 URL，测试启动、刷新、断网和返回。
8. 提交 PR 或在后台导入，并填写测试过的浏览器、设备、输入方式和构建版本。

首次接入可以直接按照[第三方游戏接入与 `.atri` 打包教程](docs/GAME_INTEGRATION.md)操作；教程包含目录结构、构建产物复制、后台导入和版本覆盖示例。

一个 PR 只提交一个新游戏或一个游戏版本，避免把门户重构与游戏收录混在一起。

## 清单字段原则

- `id`：永久标识，发布后不因改名而变化。
- `version`：游戏当前发布版本，更新游戏时递增。
- `runtime.kind`：`external` 或 `static`。
- `runtime.url`：`external` 模式的 HTTPS 入口；`runtime.entry`：`static` 模式相对于 `game/` 的 HTML 入口。
- `runtime.openIn`：使用同标签页或新标签页启动。
- `engine`：描述主要引擎/框架，不影响接入资格。
- `services`：声明是否需要联网、自有后端、实时连接和 Atri 平台集成。
- `privacy`：说明是否收集个人数据及其用途。
- `ai`：记录主要 AI 工具和人工贡献说明。

Webhook URL、签名密钥、OAuth 回调 URL 和客户端配置通过开发者控制台管理，不放入公开 Manifest。

## Pull Request 检查

- [ ] JSON 通过 Schema，未包含密钥、Token 或部署凭据。
- [ ] 启动 URL、仓库 URL、作者 URL 和媒体可访问。
- [ ] 游戏可以直接打开、刷新并正常处理浏览器返回。
- [ ] 重定向最终域名与提交者声明一致。
- [ ] 设备、输入、联网、自有后端和实时服务声明准确。
- [ ] 收集个人数据时已提供隐私政策与数据说明。
- [ ] 封面和截图是当前版本真实画面，并具有分发权利。
- [ ] 已填写 AI 辅助说明、许可证和第三方素材来源。
- [ ] 已记录桌面/移动测试结果和已知限制。

## 门户与平台代码贡献

- 修改前阅读 `docs/ARCHITECTURE.md`，保持 Catalog 与游戏运行时分离。
- Manifest、API 或 Webhook 变更先更新 Schema、类型和兼容测试。
- 不为某个游戏框架把专用运行逻辑写进门户。
- 公共 API 使用版本、Scope、请求 ID、幂等和明确错误结构。
- 新依赖说明实际用途、客户端体积影响和维护状态。
- UI 覆盖正常、加载、空数据、失效链接和错误状态。
- UI 修改验证键盘操作、窄屏、宽屏和文本溢出。

## 版本与兼容性

- Manifest Schema 使用整数版本管理。
- HTTP API 与 Webhook 事件分别版本化。
- 破坏现有清单或接口的修改必须提供迁移工具或兼容期。
- 游戏发布与 Catalog 发布相互独立；入口域名变化需要重新审核。
